name: Build Mac Executable

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:    

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller
      run: |
        pyinstaller --clean --onefile --name=run --collect-submodules=selenium --hidden-import=googleapiclient.discovery --hidden-import=google_auth_oauthlib.flow --hidden-import=google.oauth2.credentials main.py

    - name: Upload Executable Artifact
      uses: actions/upload-artifact@v4
      with:
        name: mac-executable
        path: dist/main